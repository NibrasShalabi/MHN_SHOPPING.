import 'package:flutter/material.dart';

// TODO: replace with real widgets specific to the checkout feature.
class CheckoutPlaceholderWidget extends StatelessWidget {
  const CheckoutPlaceholderWidget({super.key});

  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
