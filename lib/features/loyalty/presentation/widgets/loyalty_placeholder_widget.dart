import 'package:flutter/material.dart';

// TODO: replace with real widgets specific to the loyalty feature.
class LoyaltyPlaceholderWidget extends StatelessWidget {
  const LoyaltyPlaceholderWidget({super.key});

  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
