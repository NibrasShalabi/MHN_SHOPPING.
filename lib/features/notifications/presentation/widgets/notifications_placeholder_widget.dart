import 'package:flutter/material.dart';

// TODO: replace with real widgets specific to the notifications feature.
class NotificationsPlaceholderWidget extends StatelessWidget {
  const NotificationsPlaceholderWidget({super.key});

  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
