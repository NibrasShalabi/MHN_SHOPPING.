import 'package:flutter/material.dart';

// TODO: replace with real widgets specific to the onboarding feature.
class OnboardingPlaceholderWidget extends StatelessWidget {
  const OnboardingPlaceholderWidget({super.key});

  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
